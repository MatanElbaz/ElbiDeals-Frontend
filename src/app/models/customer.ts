export class Customer {

    public constructor(
        public firstName?: string,
        public lastName?: string,
        public id?: number,
        public password?: string,
        public email?: string,
    ) {

    }
}